@model StockManagementSystem.Models.warehouse

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>warehouse</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.warehouse_id)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.warehouse_id)
            @Html.ValidationMessageFor(model => model.warehouse_id)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.warehouse_name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.warehouse_name)
            @Html.ValidationMessageFor(model => model.warehouse_name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.warehouse_type, "Type")
        </div>
        <div class="editor-field">
            @Html.DropDownList("warehouse_type", String.Empty)
            @Html.ValidationMessageFor(model => model.warehouse_type)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
